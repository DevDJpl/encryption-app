import { Alert, Box } from '@mui/material';

//todo

export default function PageBoundary() {
    return (
        <Box className="x-center p-4">
            <Alert title="Something went wrong." />
        </Box>
    );
}
